# Use Python 3.9+ (3.8+ required; 3.7 cannot satisfy numpy/streamlit pins below).
#
# Core (Web / CLI / data layer)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# HTTP (data_fetcher: requests; East Money fallback via httpx)
requests>=2.28.0
httpx>=0.27.0

# Data sources (China equity: akshare; crypto: ccxt)
akshare>=1.13.0
# ccxt 4.5.x wheels can fail pip byte-compile on Apple Python 3.9 (invalid syntax in bundled file).
ccxt>=4.0.0,<4.5

# Web app (app.py: Streamlit + Plotly)
streamlit>=1.28.0
plotly>=5.18.0

# CLI charts (visualizer.py: mplfinance + matplotlib)
matplotlib>=3.7.0
mplfinance>=0.12.10b0

# Tests (docs/CHINA_EQUITY_VERIFICATION.md)
pytest>=7.4.0
